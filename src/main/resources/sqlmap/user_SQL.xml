<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE  mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="user">
	<insert id="insert" parameterType="com.spring.user.Member">
		<![CDATA[
			INSERT INTO member
			(username, password, authority, enabled, mname, mnickname, mphone)
			VALUES
			(#{username}, #{password}, "ROLE_USER", 1, #{mname}, #{mnickname}, #{mphone})
		]]>
	</insert>
	<select id="select" resultType="com.spring.user.Member">
		<![CDATA[
			SELECT * FROM member WHERE username=#{username}
		]]>

	</select>
	
	<select id="select_list" resultType="com.spring.user.Member">
		<![CDATA[
			SELECT * FROM member ORDER BY mid DESC
		]]>

	</select>

	<update id="update" parameterType="com.spring.user.Member">
		<![CDATA[
			UPDATE member
			SET
			mname = #{mname},
			mnickname = #{mnickname},
			mphone = #{mphone}
			WHERE username = #{username}
		]]>
	</update>

	<delete id="delete" parameterType="String">
		<![CDATA[
			DELETE FROM members
			WHERE username = #{username}
		]]>
	</delete>
</mapper>